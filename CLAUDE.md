See [AGENTS.md](AGENTS.md). It is the single source of truth for how to work on
this repo, and it applies to Claude Code as much as to any other agent.

Keep it up to date: if you change the data model, the canonical YAML format, the
validation rules, or the test strategy, update AGENTS.md and
`schema/inventory.yaml` in the same change.
