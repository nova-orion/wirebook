{
  "private": true,
  "description": "Test tooling only. wirebook itself ships as static files with no build step and no runtime dependencies.",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "test": "npm run test:core && npm run test:ui && npm run test:browser && npm run test:sweep",
    "test:core": "node test/core.test.mjs",
    "test:ui": "node test/ui.test.mjs",
    "test:browser": "node test/browser.test.mjs",
    "test:sweep": "node test/sweep.mjs",
    "explore": "node test/explore.mjs --keep-going"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
